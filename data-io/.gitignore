# Nextflow run artefacts
.nextflow/
.nextflow.log*
work/

# Benchmark output: traces, reports and the collected CSVs are the results, but
# they are large and machine-specific. Commit the summary in README.md instead.
results/*
!results/.gitkeep
